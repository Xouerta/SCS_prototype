Smart-Web-Threat-System/
├── docker-compose.yml           # [核心] 一键启动所有服务 (Node, Python, Redis, Mongo)
├── README.md                    # 项目说明文档
│
├── 📂 deploy/                   # 部署配置 (Nginx配置等，用于微前端转发)
│   └── nginx.conf
│
├── 📂 datasets/                 # 存放原始数据集 (用于训练和模拟流量)
│   ├── sql_injection.csv
│   ├── xss_payloads.csv
│   └── malicious_urls.csv
│
├── 📂 simulation/               # [核心] 流量模拟器
│   ├── generator.py             # 自动产生模拟流量脚本 (读取csv -> 发送请求)
│   └── requirements.txt         # 模拟器依赖
│
├── 📂 frontend-workspace/       # === 前端部分 (Vue3 + Vite + Qiankun) ===
│   ├── 📂 main-base/            # [主应用/基座]
│   │   ├── src/
│   │   │   ├── micro-app.js     # qiankun 微应用注册配置
│   │   │   ├── views/Login.vue  # 登录页 (含 SVG 验证码逻辑)
│   │   │   ├── layout/          # 统一的侧边栏、头部导航
│   │   │   └── store/user.js    # 用户状态管理
│   │   └── package.json
│   │
│   ├── 📂 sub-dashboard/        # [子应用1] 态势感知大屏
│   │   ├── src/
│   │   │   ├── components/
│   │   │   │   ├── AttackMap.vue    # ECharts 攻击源地图
│   │   │   │   └── TrafficWave.vue  # 流量波形图
│   │   │   ├── utils/socket.js      # WebSocket 监听封装
│   │   │   └── views/Screen.vue     # 大屏主入口
│   │   └── package.json
│   │
│   └── 📂 sub-admin/            # [子应用2] 后台管理系统
│       ├── src/
│       │   ├── views/
│       │   │   ├── LogTable.vue     # 攻击日志查询表
│       │   │   └── Settings.vue     # 系统设置
│       │   └── api/                 # 后端接口请求
│       └── package.json
│
├── 📂 backend-gateway/          # === 后端 Node.js (业务网关) ===
│   ├── src/
│   │   ├── config/              # 数据库与Redis配置
│   │   ├── controllers/
│   │   │   ├── authController.js    # 登录验证 (含 svg-captcha 生成)
│   │   │   └── logController.js     # 接收流量日志接口
│   │   ├── services/
│   │   │   ├── redisService.js      # [关键] 生产者：lpush 写入队列
│   │   │   └── websocket.js         # Socket.io 服务端逻辑
│   │   ├── models/              # MongoDB Schema (用户、日志)
│   │   ├── routes/              # 路由定义
│   │   └── app.js               # 入口文件
│   ├── package.json
│   └── .env                     # 环境变量
│
└── 📂 backend-ai-engine/        # === 后端 Python (AI 计算服务) ===
├── 📂 ml_models/            # [产物] 存放训练好的 .pkl 模型文件
│   ├── model_sqli.pkl
│   ├── model_xss.pkl
│   └── model_url.pkl
│
├── 📂 training/             # [离线] 模型训练代码 (毕设展示用)
│   ├── train_sqli.py
│   ├── train_xss.py
│   └── train_url.py
│
├── 📂 app/                  # [在线] 推理服务代码
│   ├── main.py              # FastAPI 入口
│   ├── core/
│   │   ├── predictor.py     # 加载模型并预测的逻辑
│   │   └── preprocessor.py  # 数据预处理 (分词等)
│   └── workers/
│   │   └── consumer.py      # [关键] 消费者：从 Redis 读取 -> 预测 -> 存库
│
├── requirements.txt
└── Dockerfile               # Python 环境镜像构建
